[package]
name = "models"
version = "0.1.0"
edition = "2021"
description = "Data models for the recipe extraction pipeline"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
