[package]
name = "presentation"
version = "0.1.0"
edition = "2021"
description = "Terminal rendering and file output for recipes"

[dependencies]
core = { path = "../core" }
serde_json = "1"
colored = "2"
